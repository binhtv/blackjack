package card;

import exception.BackJackException;
import exception.ExceededNumOfCardsException;
import org.junit.Assert;
import org.junit.Test;
import player.Player;

public class PlayerTest {
    @Test
    public void testGetScore() throws BackJackException {
//        Player player = new Player();
//        player.dealCard(new Card(SuitCard.CLUBS, RankCard.ACE));
//        player.dealCard(new Card(SuitCard.CLUBS, RankCard.SEVEN));
//        player.dealCard(new Card(SuitCard.CLUBS, RankCard.TEN));
//        player.dealCard(null);
//        System.out.println(player);
    }
}
